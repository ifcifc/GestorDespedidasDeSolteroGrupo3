@model IEnumerable<GestorEventos.Servicios.Entidades.EstadoEvento>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Descripcion)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Descripcion)
            </td>
            <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.IdEstadoEvento }) |
                    @Html.ActionLink("Details", "Details", new { id = item.IdEstadoEvento }) |
                    @Html.ActionLink("Delete", "Delete", new { id = item.IdEstadoEvento })
            </td>
        </tr>
}
    </tbody>
</table>
